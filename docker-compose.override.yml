version: '3'
services:
  admin-ui:
    build:
      args:
        ENV: development
    networks:
      - drill4j-dev-network
    ports:
      - 9443:443
networks:
  drill4j-dev-network:
    external:
      name: drill4j-dev-network
